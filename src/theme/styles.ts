import { Styles } from '@chakra-ui/theme-tools';

const styles: Styles = {
  global: {
    html: {
      minH: '100vh',
      bg: 'white',
      scrollBehavior: 'smooth',
    },
  },
};

export default styles;
